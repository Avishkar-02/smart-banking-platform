# Loads BEFORE application.properties and before Spring context starts.
# Must contain config-server location ? nothing else.
spring.application.name=user-service
spring.config.import=configserver:http://config-server:8888