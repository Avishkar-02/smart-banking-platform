# ================================================================
# transaction-service-docker.properties
# Place at: config-server/src/main/resources/configurations/transaction-service-docker.properties
# Layered on top of transaction-service.properties when profile=docker.
# ================================================================

spring.datasource.url=jdbc:mysql://mysql:3306/transaction_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true

spring.kafka.bootstrap-servers=kafka:9092

spring.data.redis.host=redis
spring.data.redis.port=6379

eureka.client.service-url.defaultZone=http://service-registry:8761/eureka/
